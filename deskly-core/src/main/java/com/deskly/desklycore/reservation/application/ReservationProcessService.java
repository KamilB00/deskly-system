package com.deskly.desklycore.reservation.application;

public class ReservationProcessService implements ReservationService {


    @Override
    public ReservationProcessResult reserve(ReservationProcessInput input) {
        return new ReservationProcessResult();
    }
}
